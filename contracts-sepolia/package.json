{
  "name": "firewall-margin-contracts-sepolia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "demo:trigger-stress": "hardhat run scripts/trigger-stress.ts --network sepolia",
    "demo:setup-position": "hardhat run scripts/setup-demo-position.ts --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2",
    "@types/node": "^22.0.0"
  }
}
